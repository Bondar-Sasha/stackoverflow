import { FC } from 'react'

interface LangControllerProps {
  anchorEl: null | HTMLElement
  open: boolean
  onClose: () => void
  onOpen: (e: any) => void
}

const LangController: FC<LangControllerProps> = ({}) => {
  return <div></div>
}

export default LangController
